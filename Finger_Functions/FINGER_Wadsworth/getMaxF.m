%Uses the result of 'datToMat', a data structure, to calculate the maximum
%ERD on each trial and returns as a vector
function ERD = getMaxF(dataIn)

ERD = zeros(100,1);
warning('getMaxF()is passing dummy data')

end